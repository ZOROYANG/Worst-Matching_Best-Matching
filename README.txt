match.exe是一个展示最优/最差匹配内存管理策略的示例程序。
它的输入分为两部分：命令行输入与文件输入。
命令行输入为假象内存分配或释放请求，由用户手动输入，通过在console键入match.exe可查阅输入格式。
文件输入是来自本目录下的stats.txt的数据，以二元数对的形式记录了空闲块的信息（起始地址，空闲块大小），即空闲块列表，用户可手动编辑该文件。
前提假设：stats.txt中满足4对齐且数据一致无冲突
